<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<HBox xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.example.voyeger.NavbarController"
      fx:id="navbarContainer"
      styleClass="navbar"
      alignment="CENTER_LEFT"
      spacing="5"
      prefHeight="70">

    <padding>
        <Insets top="10" right="20" bottom="10" left="20"/>
    </padding>

    <!-- Logo / Brand -->
    <HBox alignment="CENTER_LEFT" spacing="10" styleClass="brand-section">
        <Label fx:id="brandLabel" text="Voyager+ ✈️" styleClass="brand-logo"/>
    </HBox>

    <!-- Spacer -->
    <Region HBox.hgrow="ALWAYS"/>

    <!-- Navigation Buttons -->
    <HBox spacing="5" alignment="CENTER" styleClass="nav-buttons-container">

        <!-- Home -->
        <Button fx:id="homeBtn" text="🏠 Home" onAction="#handleHome" styleClass="nav-btn"/>

        <!-- Trips (Dropdown) -->
        <Button fx:id="tripsBtn" text="🌍 Trips" onAction="#handleTrips" styleClass="nav-btn nav-dropdown"/>

        <!-- Hotel Booking -->
        <Button fx:id="hotelBtn" text="🏨 Hotel" onAction="#handleHotel" styleClass="nav-btn"/>

        <!-- Tools (Dropdown) -->
        <Button fx:id="toolsBtn" text="🧰 Tools" onAction="#handleTools" styleClass="nav-btn nav-dropdown"/>

        <!-- Community (Dropdown) -->
        <Button fx:id="communityBtn" text="💬 Community" onAction="#handleCommunity" styleClass="nav-btn nav-dropdown"/>

        <!-- Notifications Button with Badge -->
        <StackPane>
            <Button text="🔔" onAction="#handleNotifications" styleClass="nav-btn"
                    style="-fx-font-size: 20px; -fx-min-width: 50px;"/>
            <!-- Notification Badge -->
            <Label fx:id="notificationBadge" text="0" styleClass="notification-badge" visible="false"
                   StackPane.alignment="TOP_RIGHT">
                <StackPane.margin>
                    <Insets top="5" right="5"/>
                </StackPane.margin>
            </Label>
        </StackPane>

        <!-- Learn & Fun (Dropdown) -->
        <Button fx:id="learnBtn" text="🎯 Learn" onAction="#handleLearn" styleClass="nav-btn nav-dropdown"/>

        <!-- Map Explorer -->
        <Button fx:id="mapBtn" text="🗺️ Map" onAction="#handleMap" styleClass="nav-btn"/>

        <!-- Profile (Dropdown) -->
        <Button fx:id="profileBtn" text="👤 Profile" onAction="#handleProfile" styleClass="nav-btn nav-dropdown"/>

    </HBox>

</HBox>
